#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <time.h> 

#define rep(x,lim) for( int x = 0 ; x < lim ; x++ ) 
#define lint long long 

#define min(a, b) ((a) < (b) ? (a) : (b)) // min function
#define max(a, b) ((a) > (b) ? (a) : (b)) // max function 
#define out(x) printf("%d " , x)
#define inp(x) scanf("%d" , &x) 
#define el printf("\n") 

void sol() {} 


int main() {

    int n ; 
    inp(n) ; 
    
    t = time(NULL) ; 
    p = time(NULL) ; 

    printf(" %d %d " , ctime(&t) , ctime(&p) ) ; 
    // while(n--) sol() ; 
    return 0 ; 
}
